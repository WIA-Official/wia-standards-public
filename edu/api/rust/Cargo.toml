[package]
name = "wia-edu"
version = "0.1.0"
edition = "2021"
authors = ["WIA <contact@wia.live>"]
description = "WIA Education Accessibility Standard - Rust API"
license = "MIT"
repository = "https://github.com/WIA-Official/wia-standards"
keywords = ["accessibility", "education", "wia", "a11y", "edtech"]
categories = ["accessibility", "education"]

[lib]
name = "wia_edu"
path = "src/lib.rs"

[dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Error handling
thiserror = "1"

# Async traits
async-trait = "0.1"

# UUID
uuid = { version = "1", features = ["v4", "serde"] }

# Date/Time
chrono = { version = "0.4", features = ["serde"] }

# URL encoding
urlencoding = "2"

# WebAssembly (optional)
wasm-bindgen = { version = "0.2", optional = true }

# Python bindings (optional)
pyo3 = { version = "0.20", features = ["extension-module"], optional = true }

[dev-dependencies]
tokio-test = "0.4"

[features]
default = []
wasm = ["wasm-bindgen"]
python = ["pyo3"]

[[example]]
name = "basic_usage"
path = "examples/basic_usage.rs"

[[example]]
name = "learner_profile"
path = "examples/learner_profile.rs"

[[test]]
name = "integration_test"
path = "tests/integration_test.rs"
