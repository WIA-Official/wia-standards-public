# WIA-RUST-LEARN 나레이션 스크립트

> YouTube @yeonsamheum 채널용
> 각 슬라이드별로 말할 내용 + 마우스로 가리킬 포인트

---

## 촬영 팁

1. **OBS 설정**: 1920x1080, 30fps
2. **마우스 하이라이터**: 마우스 위치 강조 플러그인 사용 권장
3. **속도**: 슬라이드당 30초~1분 (총 4~8분/에피소드)
4. **목소리**: 차분하게, 핵심 키워드 강조
5. **마우스**: 코드 라인 따라가며 가리키기

---

# Level 0: 시작하기

## EP 0-01: Why Rust?

### 슬라이드 1 (인트로)
**말할 것:**
> "안녕하세요, WIA Rust Learn에 오신 걸 환영합니다. 오늘은 왜 Rust를 배워야 하는지 알아볼게요."

**마우스:** 타이틀 원 그리기

---

### 슬라이드 2 (Rust란?)
**말할 것:**
> "Rust는 2015년에 1.0이 나온 시스템 프로그래밍 언어예요. Mozilla에서 만들었고, 지금은 Rust Foundation이 관리해요."

**마우스:** 연도와 "시스템 프로그래밍" 가리키기

---

### 슬라이드 3 (3가지 장점)
**말할 것:**
> "Rust의 세 가지 핵심 장점이에요. 첫째 메모리 안전성 - 가비지 컬렉터 없이도 안전해요. 둘째 제로 코스트 추상화 - C만큼 빨라요. 셋째 두려움 없는 동시성 - 데이터 레이스가 컴파일 타임에 잡혀요."

**마우스:** 각 장점을 순서대로 가리키기 (1→2→3)

---

### 슬라이드 4 (누가 쓰나?)
**말할 것:**
> "구글, 마이크로소프트, 아마존, 디스코드, 클라우드플레어 같은 대기업들이 다 쓰고 있어요. 특히 리눅스 커널에도 Rust가 들어갔어요."

**마우스:** 회사 로고/이름들 하나씩 가리키기

---

### 슬라이드 5 (어디에 쓰나?)
**말할 것:**
> "CLI 도구, 웹 서버, 운영체제, 임베디드, 게임 엔진, 블록체인까지. 시스템 레벨부터 웹 개발까지 다 가능해요."

**마우스:** 각 분야 아이콘 가리키기

---

### 슬라이드 6 (다른 언어 비교)
**말할 것:**
> "C/C++보다 안전하고, Go보다 빠르고, Python보다 타입 안전해요. 물론 배우기는 좀 더 어렵지만, 그만한 가치가 있어요."

**마우스:** 비교표 행 따라가기

---

### 슬라이드 7 (Stack Overflow)
**말할 것:**
> "Stack Overflow 설문조사에서 8년 연속 '가장 사랑받는 언어' 1위예요. 한번 배우면 다들 좋아하게 되는 언어죠."

**마우스:** 순위 그래프 가리키기

---

### 슬라이드 8 (마무리)
**말할 것:**
> "다음 영상에서는 Rust를 직접 설치해볼게요. 구독과 좋아요 부탁드려요!"

**마우스:** "다음 에피소드" 텍스트 가리키기

---

## EP 0-02: Installing Rust

### 슬라이드 1 (인트로)
> "이번엔 Rust를 설치해볼게요. 정말 쉬워요!"

### 슬라이드 2 (rustup)
> "rustup이라는 공식 설치 도구를 써요. 터미널에 이 한 줄만 치면 돼요. curl 명령어로 스크립트 다운받아서 실행하는 거예요."

**마우스:** curl 명령어 라인 따라가기

### 슬라이드 3 (Windows)
> "윈도우는 rustup-init.exe를 다운받아서 실행하면 돼요. Visual Studio C++ 빌드 도구도 같이 설치됩니다."

### 슬라이드 4 (설치 확인)
> "설치 끝나면 rustc --version 쳐보세요. 버전 나오면 성공이에요!"

**마우스:** rustc --version 명령어 가리키기

### 슬라이드 5 (VS Code)
> "에디터는 VS Code 추천해요. rust-analyzer 확장 설치하면 자동완성, 타입 힌트 다 돼요."

### 슬라이드 6 (첫 프로젝트)
> "cargo new hello_rust 치면 프로젝트가 만들어져요. cargo가 Rust의 빌드 도구이자 패키지 매니저예요."

**마우스:** 명령어와 생성되는 파일 구조 가리키기

### 슬라이드 7 (Hello World)
> "main.rs를 보면 Hello World가 이미 있어요. println! 매크로로 출력해요. 느낌표가 붙으면 매크로라는 뜻이에요."

**마우스:** println! 의 느낌표 강조

### 슬라이드 8 (실행)
> "cargo run 하면 컴파일하고 실행까지 해요. Hello, world! 나오면 성공! 다음엔 Cargo를 깊이 파볼게요."

---

## EP 0-03: Cargo Deep Dive

### 슬라이드 1 (인트로)
> "Cargo는 Rust의 심장이에요. 빌드, 테스트, 의존성 관리 다 해요."

### 슬라이드 2 (Cargo.toml)
> "Cargo.toml이 프로젝트 설정 파일이에요. 패키지 이름, 버전, 의존성을 여기 적어요."

**마우스:** [package]와 [dependencies] 섹션 가리키기

### 슬라이드 3 (주요 명령어)
> "자주 쓰는 명령어들이에요. new는 생성, build는 빌드, run은 실행, test는 테스트, check는 빠른 문법 체크예요."

**마우스:** 각 명령어 순서대로

### 슬라이드 4 (의존성 추가)
> "crates.io가 Rust의 npm이에요. dependencies에 패키지 적으면 자동으로 다운받아요."

### 슬라이드 5 (빌드 프로필)
> "cargo build는 디버그 모드, --release 붙이면 최적화된 릴리즈 빌드예요. 성능 테스트할 땐 꼭 release로!"

### 슬라이드 6 (워크스페이스)
> "큰 프로젝트는 워크스페이스로 여러 크레이트를 묶어요. 모노레포 구조로 관리할 수 있어요."

### 슬라이드 7 (유용한 명령어)
> "cargo fmt는 코드 정리, cargo clippy는 린터예요. 이거 두 개는 꼭 쓰세요!"

### 슬라이드 8 (마무리)
> "Cargo 기본 끝! 다음은 변수와 타입을 배워볼게요."

---

# Level 1: 기초

## EP 1-01: Variables & Types

### 슬라이드 1 (인트로)
> "Rust의 변수와 타입을 배워볼게요. Rust는 기본이 불변이에요!"

### 슬라이드 2 (let과 불변)
> "let으로 변수 만들어요. 근데 기본이 불변이에요! 바꾸려고 하면 컴파일 에러나요."

**마우스:** let x = 5; 그리고 에러 메시지 가리키기

### 슬라이드 3 (mut)
> "바꾸고 싶으면 mut를 붙여요. let mut x 하면 나중에 값을 바꿀 수 있어요."

**마우스:** mut 키워드 강조

### 슬라이드 4 (기본 타입)
> "정수는 i32, u64 같은 거, 실수는 f32, f64, 불린은 bool, 문자는 char이에요."

**마우스:** 각 타입 하나씩

### 슬라이드 5 (타입 추론)
> "Rust는 타입 추론이 있어요. 대부분 안 적어도 알아서 추론해요. 근데 명시해주는 게 좋을 때도 있어요."

### 슬라이드 6 (상수)
> "const는 컴파일 타임 상수예요. 전부 대문자로 쓰고, 타입을 꼭 명시해야 해요."

**마우스:** const MAX_POINTS: u32 = 100_000;

### 슬라이드 7 (섀도잉)
> "같은 이름으로 다시 let 하면 섀도잉이에요. 타입도 바꿀 수 있어요. mut랑은 달라요!"

### 슬라이드 8 (마무리)
> "변수와 타입 끝! 다음은 함수와 제어 흐름이에요."

---

## EP 1-02: Functions & Control Flow

### 슬라이드 1 (인트로)
> "함수와 제어 흐름을 배워볼게요!"

### 슬라이드 2 (함수 정의)
> "fn으로 함수 만들어요. 파라미터 타입은 꼭 명시해야 해요."

**마우스:** fn add(a: i32, b: i32) 부분

### 슬라이드 3 (반환값)
> "화살표로 반환 타입 적고, 마지막 표현식이 반환값이에요. 세미콜론 없으면 반환!"

**마우스:** -> i32 그리고 마지막 줄의 세미콜론 없는 부분

### 슬라이드 4 (if 표현식)
> "if는 표현식이에요! 그래서 let result = if ... else 가능해요. 조건에 괄호 안 써도 돼요."

### 슬라이드 5 (loop)
> "loop은 무한 루프예요. break로 탈출하고, break에 값을 붙이면 그게 반환값이에요."

**마우스:** break counter * 2 부분

### 슬라이드 6 (while)
> "while은 조건부 루프예요. 조건이 true인 동안 계속 돌아요."

### 슬라이드 7 (for)
> "for가 제일 많이 써요. 이터레이터 돌릴 때 사용해요. 1..5는 1,2,3,4고 1..=5는 1,2,3,4,5예요."

**마우스:** 1..5 와 1..=5 차이점

### 슬라이드 8 (마무리)
> "함수와 제어 흐름 끝! 다음은 문자열이에요. String과 &str의 차이!"

---

## EP 1-03: String vs &str

### 슬라이드 1 (인트로)
> "Rust의 문자열은 두 종류가 있어요. 처음엔 헷갈리지만 중요해요!"

### 슬라이드 2 (두 가지 문자열)
> "String은 힙에 있는 가변 문자열, &str은 문자열 슬라이스예요. 소유 vs 빌림의 차이!"

**마우스:** String과 &str 비교 부분

### 슬라이드 3 (&str - 문자열 리터럴)
> "큰따옴표로 쓰면 &str이에요. 바이너리에 박혀있는 정적 문자열이에요."

### 슬라이드 4 (String 생성)
> "String은 String::from이나 .to_string()으로 만들어요. 힙에 할당되고 수정 가능해요."

**마우스:** 두 가지 생성 방법 가리키기

### 슬라이드 5 (String 수정)
> "push_str로 문자열 추가, push로 문자 하나 추가해요. &str은 수정 못 해요!"

### 슬라이드 6 (변환)
> "String을 &str로? &만 붙이면 돼요. &str을 String으로? .to_string() 해요."

### 슬라이드 7 (함수 파라미터)
> "함수 파라미터는 보통 &str로 받아요. 그러면 String도 &str도 다 받을 수 있어요."

**마우스:** fn greet(name: &str) 부분

### 슬라이드 8 (마무리)
> "핵심: String은 소유, &str은 빌림! 다음은 컬렉션이에요."

---

## EP 1-04: Collections

### 슬라이드 1 (인트로)
> "Vec, HashMap 같은 컬렉션을 배워볼게요!"

### 슬라이드 2 (Vec 생성)
> "Vec은 동적 배열이에요. vec! 매크로나 Vec::new()로 만들어요."

**마우스:** vec![1, 2, 3] 부분

### 슬라이드 3 (Vec 조작)
> "push로 추가, pop으로 제거, get으로 안전하게 접근해요. 인덱스로도 접근 가능해요."

### 슬라이드 4 (Vec 순회)
> "for in으로 순회해요. &vec은 불변 빌림, &mut vec은 가변 빌림이에요."

### 슬라이드 5 (HashMap)
> "HashMap은 키-값 저장이에요. use std::collections::HashMap; 해줘야 해요."

### 슬라이드 6 (HashMap 조작)
> "insert로 추가, get으로 조회해요. get은 Option을 반환해요!"

### 슬라이드 7 (이터레이터)
> "iter(), map(), filter(), collect() 같은 이터레이터 메서드가 강력해요. 함수형 스타일로 코딩할 수 있어요."

**마우스:** 체이닝 부분 따라가기

### 슬라이드 8 (마무리)
> "컬렉션 끝! 다음은 Rust의 핵심, 소유권이에요! 가장 중요한 파트니까 집중하세요!"

---

# Level 2: 소유권 (NRT)

## EP 2-01: Ownership Rules ⭐

### 슬라이드 1 (인트로)
> "Rust의 핵심! 소유권이에요. 이것만 이해하면 Rust의 80%는 끝이에요!"

### 슬라이드 2 (소유권 3규칙)
> "세 가지 규칙이에요. 첫째, 모든 값은 소유자가 있다. 둘째, 소유자는 하나뿐이다. 셋째, 소유자가 스코프 벗어나면 값이 해제된다."

**마우스:** 1, 2, 3 순서대로 강조

### 슬라이드 3 (NRT 개념)
> "제가 소유권을 NFT처럼 생각해봤어요. NRT - Non-Repeatable Transfer! 한 번 주면 끝, 복사 안 되는 토큰!"

### 슬라이드 4 (스코프)
> "중괄호가 스코프예요. 변수가 스코프 벗어나면 drop 호출되고 메모리 해제돼요."

**마우스:** 중괄호 시작과 끝 가리키기

### 슬라이드 5 (왜 필요할까?)
> "이게 왜 좋으냐? GC 없이 메모리 안전! 댕글링 포인터, 더블 프리 같은 버그를 컴파일 타임에 잡아요."

### 슬라이드 6 (Stack vs Heap)
> "스택은 빠르고 고정 크기, 힙은 느리지만 동적이에요. String은 힙에, 정수는 스택에!"

**마우스:** 메모리 다이어그램

### 슬라이드 7 (Drop)
> "스코프 끝나면 자동으로 drop 호출! 메모리 자동 해제, 파일 자동 닫기. RAII 패턴이에요."

### 슬라이드 8 (마무리)
> "소유권 기본 끝! 다음은 Move, Copy, Clone 차이를 배워요!"

---

## EP 2-02: Move, Copy, Clone

### 슬라이드 1 (인트로)
> "Move가 뭔지, Copy 타입은 뭔지, Clone은 언제 쓰는지 알아봐요!"

### 슬라이드 2 (Move)
> "String 같은 타입은 대입하면 Move! s1을 s2에 넣으면 s1은 더 이상 못 써요."

**마우스:** let s2 = s1; 부분과 에러 메시지

### 슬라이드 3 (왜 Move?)
> "힙 데이터를 두 변수가 가리키면? 스코프 끝날 때 둘 다 해제해서 더블 프리 버그! Move로 방지해요."

### 슬라이드 4 (Copy 타입)
> "정수, 불린, 문자 같은 건 Copy 타입! 스택에 있고 복사 비용이 싸서 자동 복사돼요."

**마우스:** i32, bool, char 가리키기

### 슬라이드 5 (Clone)
> "힙 데이터를 진짜 복사하고 싶으면? .clone() 쓰세요. 명시적으로 비용 지불!"

**마우스:** s1.clone() 부분

### 슬라이드 6 (함수 전달)
> "함수에 String 넘기면 Move돼요! 함수 끝나면 사라져요. 다시 쓰고 싶으면 반환받거나 참조를 넘겨야 해요."

### 슬라이드 7 (비교 표)
> "Move는 소유권 이전, Copy는 자동 복사, Clone은 명시적 깊은 복사! 이 차이 기억하세요."

**마우스:** 표 행 따라가기

### 슬라이드 8 (마무리)
> "다음은 참조와 빌림! 소유권 안 넘기고 빌려주는 방법이에요."

---

## EP 2-03: References & Borrowing

### 슬라이드 1 (인트로)
> "소유권 안 넘기고 빌려주기! 참조를 배워봐요."

### 슬라이드 2 (참조란)
> "앰퍼샌드 &로 참조를 만들어요. 소유권은 그대로, 값만 빌려주는 거예요."

**마우스:** &s1 부분

### 슬라이드 3 (불변 참조)
> "&s는 불변 참조예요. 읽기만 가능! 여러 개 동시에 가능해요."

### 슬라이드 4 (가변 참조)
> "&mut s는 가변 참조예요. 수정 가능! 근데 딱 하나만 가능해요."

**마우스:** &mut 강조

### 슬라이드 5 (빌림 규칙)
> "불변 참조 N개 OR 가변 참조 1개! 둘 다는 안 돼요. 데이터 레이스 방지!"

### 슬라이드 6 (댕글링 방지)
> "참조가 원본보다 오래 살면? 컴파일 에러! 댕글링 포인터 없어요."

### 슬라이드 7 (함수에서 빌림)
> "함수 파라미터로 &String 받으면 빌리는 거예요. 함수 끝나도 원본 살아있어요!"

**마우스:** fn calculate_length(s: &String) 부분

### 슬라이드 8 (마무리)
> "빌림 끝! 다음은 라이프타임, 참조가 얼마나 오래 살지 정하는 거예요."

---

## EP 2-04: Lifetimes

### 슬라이드 1 (인트로)
> "라이프타임! 참조가 얼마나 오래 유효한지 컴파일러에게 알려주는 거예요."

### 슬라이드 2 (왜 필요?)
> "두 참조 중 어떤 게 더 오래 살지? 컴파일러가 모를 때 우리가 알려줘야 해요."

### 슬라이드 3 (문법)
> "작은따옴표 a, 'a 이렇게 써요. 제네릭처럼 꺾쇠에 넣어요."

**마우스:** <'a> 부분

### 슬라이드 4 (함수 시그니처)
> "파라미터와 반환값의 라이프타임을 연결해요. 반환값이 어떤 파라미터만큼 산다는 뜻!"

**마우스:** fn longest<'a>(x: &'a str, y: &'a str) -> &'a str

### 슬라이드 5 (라이프타임 생략)
> "간단한 경우는 안 적어도 돼요. 컴파일러가 추론해줘요. 규칙 세 가지가 있어요."

### 슬라이드 6 (구조체에서)
> "구조체가 참조를 가지면? 라이프타임 명시해야 해요."

**마우스:** struct ImportantExcerpt<'a> 부분

### 슬라이드 7 ('static)
> "'static은 프로그램 전체 동안 사는 라이프타임이에요. 문자열 리터럴이 이거예요."

### 슬라이드 8 (마무리)
> "라이프타임 어렵죠? 일단 컴파일러가 시키는 대로 하면 돼요! 다음은 구조체예요."

---

# Level 3: 구조화

## EP 3-01: Structs & Enums

### 슬라이드 1 (인트로)
> "데이터를 구조화하는 방법! Struct와 Enum이에요."

### 슬라이드 2 (Struct 정의)
> "struct로 필드들을 묶어요. 각 필드에 이름과 타입을 정해요."

**마우스:** struct User { name: String, age: u32 }

### 슬라이드 3 (인스턴스 생성)
> "필드 이름과 값을 매핑해서 만들어요. 순서는 상관없어요."

### 슬라이드 4 (메서드 impl)
> "impl 블록에서 메서드 만들어요. 첫 파라미터는 &self - 자기 자신 참조!"

**마우스:** impl User { fn greet(&self) }

### 슬라이드 5 (Enum 정의)
> "enum은 여러 가능성 중 하나! 각 variant가 다른 타입 가질 수 있어요."

### 슬라이드 6 (Option)
> "Option<T>가 대표적이에요. Some(값) 또는 None! null 대신 써요."

**마우스:** Option::Some과 Option::None

### 슬라이드 7 (Result)
> "Result<T, E>는 성공 또는 실패! Ok(값) 또는 Err(에러). 에러 처리의 핵심이에요."

### 슬라이드 8 (마무리)
> "Struct로 묶고 Enum으로 분기! 다음은 에러 처리예요."

---

## EP 3-02: Error Handling

### 슬라이드 1 (인트로)
> "Rust의 에러 처리! panic과 Result를 배워요."

### 슬라이드 2 (panic!)
> "panic!은 복구 불가 에러예요. 프로그램 죽어요. 정말 심각할 때만 써요."

### 슬라이드 3 (Result)
> "대부분은 Result로 처리해요. Ok면 성공, Err면 실패!"

**마우스:** Result<T, E> 구조

### 슬라이드 4 (match로 처리)
> "match로 Ok와 Err 각각 처리해요. 모든 케이스 다 처리해야 해요."

### 슬라이드 5 (? 연산자)
> "물음표 ?가 마법이에요! 에러면 바로 반환, 성공이면 값 꺼내기. 코드가 깔끔해져요."

**마우스:** ? 연산자 강조

### 슬라이드 6 (unwrap과 expect)
> "unwrap은 에러면 panic, expect는 메시지 붙여서 panic. 프로토타이핑할 때 써요."

### 슬라이드 7 (커스텀 에러)
> "thiserror나 anyhow 크레이트 쓰면 에러 타입 쉽게 만들어요."

### 슬라이드 8 (마무리)
> "? 연산자 사랑하세요! 다음은 모듈 시스템이에요."

---

## EP 3-03: Modules & Crates

### 슬라이드 1 (인트로)
> "코드 정리하기! 모듈과 크레이트를 배워요."

### 슬라이드 2 (mod 키워드)
> "mod로 모듈 만들어요. 중괄호 안에 넣거나 파일로 분리해요."

**마우스:** mod my_module { }

### 슬라이드 3 (pub 키워드)
> "기본은 private! pub 붙여야 외부에서 접근 가능해요."

### 슬라이드 4 (use 키워드)
> "use로 경로 단축해요. std::collections::HashMap을 HashMap으로!"

**마우스:** use std::collections::HashMap;

### 슬라이드 5 (파일 구조)
> "mod.rs 또는 파일명.rs로 모듈 분리해요. 2018 에디션부터는 파일명.rs가 권장!"

### 슬라이드 6 (크레이트)
> "크레이트는 컴파일 단위예요. binary 크레이트와 library 크레이트가 있어요."

### 슬라이드 7 (crates.io)
> "crates.io에서 외부 크레이트 가져와요. Cargo.toml에 추가하면 끝!"

### 슬라이드 8 (마무리)
> "모듈로 정리, 크레이트로 공유! 다음은 제네릭과 트레이트예요."

---

# Level 4: 고급 기능

## EP 4-01: Generics & Traits

### 슬라이드 1 (인트로)
> "제네릭으로 타입 추상화, 트레이트로 행동 정의!"

### 슬라이드 2 (제네릭 함수)
> "꺾쇠 안에 T 넣으면 어떤 타입이든 받아요!"

**마우스:** fn largest<T>(list: &[T]) -> T

### 슬라이드 3 (제네릭 구조체)
> "구조체에도 제네릭! Point<T>면 정수든 실수든 다 돼요."

### 슬라이드 4 (트레이트 정의)
> "trait은 인터페이스같은 거예요. 메서드 시그니처를 정의해요."

**마우스:** trait Summary { fn summarize(&self) -> String; }

### 슬라이드 5 (트레이트 구현)
> "impl Trait for Type으로 구현해요. 구조체에 행동을 붙이는 거예요."

### 슬라이드 6 (트레이트 바운드)
> "T: Display 이러면 Display 트레이트 구현한 타입만 받아요."

**마우스:** <T: Display + Debug>

### 슬라이드 7 (derive)
> "#[derive(...)]로 자동 구현! Debug, Clone, PartialEq 많이 써요."

### 슬라이드 8 (마무리)
> "제네릭과 트레이트는 Rust의 핵심! 다음은 클로저예요."

---

## EP 4-02: Closures

### 슬라이드 1 (인트로)
> "클로저! 익명 함수인데 환경을 캡처해요."

### 슬라이드 2 (클로저 문법)
> "파이프로 파라미터, 화살표 없이 바로 본문! |x| x + 1 이렇게요."

**마우스:** |x| x + 1 문법 부분

### 슬라이드 3 (환경 캡처)
> "바깥 변수를 가져다 쓸 수 있어요. 함수랑 다른 점!"

### 슬라이드 4 (캡처 모드)
> "빌림, 가변 빌림, 소유권 가져가기 세 가지예요. 컴파일러가 알아서 선택!"

### 슬라이드 5 (move 키워드)
> "move 붙이면 강제로 소유권 가져가요. 스레드에 넘길 때 자주 써요."

**마우스:** move |x| 부분

### 슬라이드 6 (Fn 트레이트)
> "Fn, FnMut, FnOnce 세 가지 트레이트! 클로저를 파라미터로 받을 때 써요."

### 슬라이드 7 (이터레이터와)
> "map, filter에 클로저 넘기면 함수형 스타일! 읽기 좋고 강력해요."

### 슬라이드 8 (마무리)
> "클로저는 이터레이터랑 짝꿍! 다음은 스마트 포인터예요."

---

## EP 4-03: Smart Pointers

### 슬라이드 1 (인트로)
> "스마트 포인터! Box, Rc, RefCell을 배워요."

### 슬라이드 2 (Box<T>)
> "Box는 힙에 할당해요. 재귀 타입이나 큰 데이터에 써요."

**마우스:** Box::new(value)

### 슬라이드 3 (Deref)
> "스마트 포인터도 *로 역참조 가능! Deref 트레이트 덕분이에요."

### 슬라이드 4 (Rc<T>)
> "Rc는 참조 카운팅! 여러 소유자가 가능해요. 단일 스레드에서만!"

**마우스:** Rc::clone(&rc)

### 슬라이드 5 (RefCell<T>)
> "RefCell은 런타임 빌림 체크! 컴파일 타임에 못 잡는 케이스용."

### 슬라이드 6 (내부 가변성)
> "불변 참조인데 안에서 바꾸고 싶을 때! RefCell<T>로 가능해요."

### 슬라이드 7 (조합)
> "Rc<RefCell<T>>면 여러 소유자가 수정 가능! 트리 구조에 좋아요."

### 슬라이드 8 (마무리)
> "Box는 힙, Rc는 공유, RefCell은 내부 가변성! 다음은 동시성이에요."

---

# Level 5: 동시성

## EP 5-01: Threads & Channels

### 슬라이드 1 (인트로)
> "두려움 없는 동시성! 스레드와 채널을 배워요."

### 슬라이드 2 (스레드 생성)
> "thread::spawn으로 스레드 만들어요. 클로저 넘기면 돼요."

**마우스:** thread::spawn(|| { })

### 슬라이드 3 (join)
> "handle.join()으로 스레드 끝날 때까지 기다려요."

### 슬라이드 4 (move 클로저)
> "스레드에 데이터 넘기려면 move! 소유권 넘겨야 안전해요."

**마우스:** move || 부분

### 슬라이드 5 (채널)
> "mpsc::channel로 메시지 패싱! tx로 보내고 rx로 받아요."

### 슬라이드 6 (Arc<Mutex<T>>)
> "여러 스레드가 공유하려면? Arc로 공유 소유권, Mutex로 락!"

**마우스:** Arc::new(Mutex::new(data))

### 슬라이드 7 (Send와 Sync)
> "Send면 다른 스레드로 보내기 가능, Sync면 여러 스레드에서 참조 가능!"

### 슬라이드 8 (마무리)
> "컴파일러가 데이터 레이스 막아줘요! 다음은 async/await!"

---

## EP 5-02: Async/Await

### 슬라이드 1 (인트로)
> "비동기 프로그래밍! async와 await를 배워요."

### 슬라이드 2 (async fn)
> "async fn은 Future를 반환해요. 바로 실행 안 되고 .await 해야 해요."

**마우스:** async fn fetch() -> Result<...>

### 슬라이드 3 (await)
> ".await가 기다리는 지점이에요. 다른 작업이 그동안 실행돼요."

**마우스:** .await 부분 강조

### 슬라이드 4 (런타임)
> "Tokio가 대표적 런타임! #[tokio::main]으로 시작해요."

### 슬라이드 5 (동시 실행)
> "join!으로 여러 Future를 동시에! 다 끝날 때까지 기다려요."

### 슬라이드 6 (spawn)
> "tokio::spawn으로 태스크 생성! 백그라운드에서 돌아요."

### 슬라이드 7 (언제 쓰나)
> "I/O 바운드 작업에 좋아요. 네트워크, 파일 시스템 같은 거요."

### 슬라이드 8 (마무리)
> "async는 I/O, thread는 CPU! 다음은 테스팅이에요."

---

# Level 6: 테스팅 & Cargo

## EP 6-01: Testing in Rust

### 슬라이드 1 (인트로)
> "테스트는 기본! Rust 내장 테스트를 배워요."

### 슬라이드 2 (#[test])
> "#[test] 붙이면 테스트 함수예요. cargo test로 실행!"

**마우스:** #[test] 어트리뷰트

### 슬라이드 3 (assert 매크로)
> "assert!, assert_eq!, assert_ne! 세 가지! 조건 검사해요."

### 슬라이드 4 (should_panic)
> "#[should_panic]은 패닉해야 통과! 에러 케이스 테스트용."

### 슬라이드 5 (Result)
> "테스트 함수가 Result 반환 가능! Err면 실패예요."

### 슬라이드 6 (테스트 구성)
> "#[cfg(test)] 모듈로 테스트 코드 분리! 빌드에 안 들어가요."

### 슬라이드 7 (통합 테스트)
> "tests/ 폴더에 넣으면 통합 테스트! 외부 관점으로 테스트해요."

### 슬라이드 8 (마무리)
> "테스트 많이 쓰세요! 다음은 Cargo 고급 기능이에요."

---

## EP 6-02: Cargo Advanced

### 슬라이드 1 (인트로)
> "Cargo 고급 기능! 워크스페이스, 피처, 퍼블리싱!"

### 슬라이드 2 (워크스페이스)
> "여러 크레이트를 하나로! 루트 Cargo.toml에 members 적어요."

**마우스:** [workspace] members = [...]

### 슬라이드 3 (피처)
> "feature로 조건부 컴파일! 옵션 기능 켜고 끄기 가능해요."

### 슬라이드 4 (빌드 프로필)
> "dev, release 프로필 커스텀! 최적화 레벨 바꿀 수 있어요."

### 슬라이드 5 (빌드 스크립트)
> "build.rs로 빌드 전 작업! C 라이브러리 연동할 때 써요."

### 슬라이드 6 (문서화)
> "cargo doc으로 문서 생성! /// 주석이 문서가 돼요."

### 슬라이드 7 (퍼블리싱)
> "cargo publish로 crates.io에 배포! 세상과 공유해요."

### 슬라이드 8 (마무리)
> "Cargo 마스터! 다음은 패턴 매칭 고급편이에요."

---

# Level 7: 패턴 & OOP

## EP 7-01: Pattern Matching

### 슬라이드 1 (인트로)
> "패턴 매칭 고급! 더 다양한 패턴을 배워요."

### 슬라이드 2 (구조체 분해)
> "구조체 필드를 바로 꺼내요! let Point { x, y } = point;"

**마우스:** 분해 문법

### 슬라이드 3 (튜플 분해)
> "튜플도 분해! let (a, b, c) = tuple;"

### 슬라이드 4 (가드)
> "if 조건 추가! match arm에 조건 붙여요."

**마우스:** Some(x) if x > 5 =>

### 슬라이드 5 (@ 바인딩)
> "@로 매칭하면서 바인딩! 값도 검사하고 변수에도 넣어요."

### 슬라이드 6 (범위 패턴)
> "1..=5 범위 매칭 가능! 여러 값 한번에 처리해요."

### 슬라이드 7 (if let, while let)
> "한 패턴만 처리할 때! match보다 간결해요."

### 슬라이드 8 (마무리)
> "패턴 매칭 강력하죠? 다음은 Rust 스타일 OOP예요."

---

## EP 7-02: OOP in Rust

### 슬라이드 1 (인트로)
> "Rust에서 객체지향! 상속 없이 하는 법이에요."

### 슬라이드 2 (캡슐화)
> "pub으로 공개 범위 정해요. 필드는 숨기고 메서드로 접근!"

### 슬라이드 3 (트레이트 = 인터페이스)
> "trait이 인터페이스 역할! 공통 행동을 정의해요."

### 슬라이드 4 (다형성)
> "dyn Trait로 동적 디스패치! 런타임에 메서드 결정!"

**마우스:** Box<dyn Animal>

### 슬라이드 5 (상속 대신 조합)
> "상속 없어요! 대신 조합으로 코드 재사용해요."

### 슬라이드 6 (디폴트 구현)
> "trait에 기본 구현 가능! 필요하면 오버라이드해요."

### 슬라이드 7 (타입 상태 패턴)
> "제네릭으로 상태 머신! 컴파일 타임에 상태 체크해요."

### 슬라이드 8 (마무리)
> "상속 없이도 충분해요! 다음은 unsafe Rust예요."

---

# Level 8: Unsafe & 매크로

## EP 8-01: Unsafe Rust

### 슬라이드 1 (인트로)
> "Unsafe! 컴파일러 보호막 벗기! 신중하게 써야 해요."

### 슬라이드 2 (5가지 슈퍼파워)
> "unsafe 안에서만 가능한 다섯 가지! 로우 포인터, static mut, unsafe fn, extern, unsafe trait."

**마우스:** 5가지 하나씩

### 슬라이드 3 (로우 포인터)
> "*const T와 *mut T! 참조처럼 역참조하는데 제약 없어요."

### 슬라이드 4 (역참조)
> "unsafe 블록 안에서 역참조! 밖에서 만들어도 돼요."

### 슬라이드 5 (unsafe fn)
> "unsafe fn은 호출자가 책임져요! 사전 조건 만족해야 해요."

### 슬라이드 6 (FFI)
> "extern으로 C 함수 호출! 다른 언어와 연동해요."

**마우스:** extern "C" { }

### 슬라이드 7 (static mut)
> "전역 가변 변수! 데이터 레이스 위험 있어서 unsafe예요."

### 슬라이드 8 (마무리)
> "unsafe는 최소한으로! 다음은 매크로예요."

---

## EP 8-02: Macros

### 슬라이드 1 (인트로)
> "매크로! 코드를 생성하는 코드예요."

### 슬라이드 2 (선언적 매크로)
> "macro_rules!로 정의! 패턴 매칭으로 확장해요."

**마우스:** macro_rules! my_macro { }

### 슬라이드 3 (반복)
> "$( )* 로 반복! vec![1,2,3] 같은 거 가능해요."

### 슬라이드 4 (절차적 매크로)
> "derive 매크로! #[derive(Debug)] 같은 거예요."

### 슬라이드 5 (커스텀 derive)
> "proc-macro 크레이트로 직접 만들어요! TokenStream 다뤄요."

### 슬라이드 6 (속성 매크로)
> "#[route(GET, "/")] 같은 거! 함수에 속성 붙여요."

### 슬라이드 7 (함수형 매크로)
> "sql!(SELECT * FROM users) 같은 거! 파싱해서 코드 생성!"

### 슬라이드 8 (마무리)
> "매크로는 강력하지만 복잡해요! 다음은 실전 프로젝트!"

---

# Level 9: 실전 프로젝트

## EP 9-01: CLI App

### 슬라이드 1 (인트로)
> "CLI 앱 만들기! clap으로 쉽게 해요."

### 슬라이드 2 (clap 설정)
> "Cargo.toml에 clap 추가! derive feature 켜면 편해요."

### 슬라이드 3 (구조체로 정의)
> "#[derive(Parser)]로 인자 구조체! 타입으로 자동 파싱!"

**마우스:** #[derive(Parser)] struct Args

### 슬라이드 4 (서브커맨드)
> "enum으로 서브커맨드! git add, git commit 처럼요."

### 슬라이드 5 (파일 처리)
> "std::fs로 파일 읽고 쓰기! 에러는 ?로 처리해요."

### 슬라이드 6 (컬러 출력)
> "colored 크레이트로 예쁘게! "Hello".green() 이렇게요."

### 슬라이드 7 (에러 처리)
> "anyhow로 에러 관리! 여러 에러 타입 통합해요."

### 슬라이드 8 (마무리)
> "CLI 앱 완성! 다음은 Web API예요."

---

## EP 9-02: Web API

### 슬라이드 1 (인트로)
> "REST API 만들기! Axum 프레임워크 써요."

### 슬라이드 2 (Axum 설정)
> "tokio와 axum 추가! async main으로 시작해요."

### 슬라이드 3 (라우터)
> "Router::new().route()로 경로 등록! 핸들러 연결해요."

**마우스:** Router::new().route("/", get(handler))

### 슬라이드 4 (핸들러)
> "async fn이 핸들러! Json으로 응답해요."

### 슬라이드 5 (경로 파라미터)
> "Path<id>로 URL 파라미터 추출! /users/:id 같은 거요."

### 슬라이드 6 (상태 공유)
> "State로 앱 상태 공유! Arc<Mutex<..>> 패턴 써요."

### 슬라이드 7 (미들웨어)
> "tower 미들웨어로 로깅, 인증! layer로 붙여요."

### 슬라이드 8 (마무리)
> "API 서버 완성! 다음은 시스템 도구예요."

---

## EP 9-03: System Tool

### 슬라이드 1 (인트로)
> "시스템 모니터링 도구! 마지막 프로젝트예요."

### 슬라이드 2 (파일 시스템)
> "std::fs로 파일, 디렉토리 다뤄요. walkdir로 재귀 탐색!"

### 슬라이드 3 (프로세스)
> "std::process::Command로 외부 명령 실행! 출력 캡처해요."

**마우스:** Command::new("ls").output()

### 슬라이드 4 (환경변수)
> "std::env로 환경변수 읽기! 설정 파일 대신 쓰기도 해요."

### 슬라이드 5 (sysinfo)
> "sysinfo 크레이트로 시스템 정보! CPU, 메모리, 프로세스."

### 슬라이드 6 (종합 예제)
> "시스템 모니터 만들어요! CPU 사용량 실시간 출력!"

### 슬라이드 7 (크로스 플랫폼)
> "cfg 매크로로 OS별 코드! Windows, Linux, Mac 지원해요."

### 슬라이드 8 (축하!)
> "28편 모두 완료! 이제 여러분은 Rust 개발자예요! 구독과 좋아요 부탁드려요!"

**마우스:** 축하 메시지 원 그리기, 하트 그리기

---

# 보너스: 짧은 나레이션 템플릿

## 인트로 (모든 에피소드)
> "안녕하세요, WIA Rust Learn [에피소드 번호]입니다. 오늘은 [주제]를 배워볼게요!"

## 아웃트로 (모든 에피소드)
> "오늘 [주제] 배웠어요! 다음 영상에서는 [다음 주제]를 다룰게요. 구독과 좋아요 부탁드려요!"

## 코드 설명할 때
> "이 부분 보세요..." (마우스로 가리키며)
> "여기서 중요한 건..." (핵심 키워드 강조)
> "이게 왜 이런지 설명하면..." (이유 설명)

## 에러 설명할 때
> "여기서 에러가 나는 이유는..."
> "컴파일러가 뭐라고 하냐면..."
> "이걸 고치려면..."

---

# 연습 사이트 연계 스크립트

## 에피소드 끝에 추가할 말

### Level 2 (소유권) 에피소드
> "직접 연습해보고 싶으면 우리 사이트의 Ownership Visualizer 써보세요! 메모리가 어떻게 움직이는지 눈으로 볼 수 있어요."

### 코드 연습 권장 에피소드
> "직접 코드 쳐보고 싶으면 Playground 써보세요! 실제 Rust가 돌아가요."

### 이북 추천
> "더 자세한 설명은 Ebook에서 볼 수 있어요!"

---

© 2025 WIA / @yeonsamheum
弘益人間 - 널리 인간을 이롭게 하라
