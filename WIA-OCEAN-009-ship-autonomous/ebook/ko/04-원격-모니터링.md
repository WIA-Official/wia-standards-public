# 4장: 원격 모니터링 및 제어

## 육상 기반 운영 센터

자율운항선박은 독립적으로 운영할 수 있지만, 원격 모니터링 및 제어 기능은 인간의 감독, 필요 시 개입 및 운영 효율성을 보장합니다. 육상 제어 센터(SCC)는 항공 교통 관제의 해양 버전으로, 함대를 조정하고, 조건을 모니터링하며, 전문가 지원을 제공합니다.

### 육상 제어 센터 아키텍처

현대 육상 제어 센터는 여러 자율 선박을 동시에 관리하여 모니터링, 의사결정 지원 및 원격 제어 기능을 제공합니다.

**SCC 구성 요소:**

```typescript
interface ShoreControlCenter {
  // 함대 관리
  fleetMonitor: FleetMonitoringSystem;
  vesselControlStations: VesselControlStation[];

  // 통신 인프라
  satelliteNetwork: SatelliteCommsManager;
  terrestrialNetwork: 5GNetworkManager;
  backupComms: HFRadioSystem;

  // 의사결정 지원
  weatherService: WeatherInformationService;
  trafficService: MarineTrafficService;
  routeOptimizer: RouteOptimizationEngine;

  // 인력
  operators: RemoteOperator[];
  fleetManager: FleetManager;
  technicalSupport: TechnicalSupportTeam;
}

interface VesselControlStation {
  vesselId: string;
  operator: RemoteOperator;

  // 디스플레이 시스템
  primaryDisplays: Display[];  // 6-8개의 대형 모니터
  navigationChart: ElectronicChartDisplay;
  cameraFeeds: CameraFeedDisplay;
  sensorVisualization: SensorDataDisplay;

  // 제어 인터페이스
  manualControl: ManualControlInterface;
  autopilotOverride: AutopilotOverrideControls;
  emergencyStop: EmergencyStopButton;

  // 통신
  vesselComms: VesselCommunicationChannel;
  voice: VoiceChannelToShip;

  // 데이터 피드
  telemetry: TelemetryDataStream;
  sensorData: SensorDataStream;
  systemStatus: SystemStatusFeed;
}
```

### 통신 시스템

안정적이고 높은 대역폭의 통신은 원격 운영에 중요합니다.

#### 위성 통신

**VSAT (Very Small Aperture Terminal):**
```typescript
interface VSATSystem {
  provider: "Inmarsat" | "Iridium" | "Starlink";
  terminal: {
    antennaSize: number;        // 센티미터 (일반적으로 60-120cm)
    transmitPower: number;      // 와트
    stabilization: "3-axis";    // 선박 움직임을 위한 자이로 안정화
  };

  bandwidth: {
    download: number;           // Mbps (2-200 Mbps)
    upload: number;             // Mbps (1-100 Mbps)
    latency: number;            // 밀리초 (GEO의 경우 500-800)
  };

  coverage: "global" | "regional";
  redundancy: VSATSystem[];     // 백업 시스템
}
```

**Starlink Maritime:**
- 저궤도(LEO) 위성군
- 100-200 Mbps 다운로드 속도
- 50-150ms 지연 (기존 GEO 위성의 500-800ms 대비)
- 전 세계 커버리지 (확장 진행 중)
- 위상 배열 안테나 (전자식 조향, 움직이는 부품 없음)
- 월 비용: 500만-1,000만 원 (2025년)

#### 연안 운영을 위한 5G/LTE

```typescript
interface CellularNetwork {
  technology: "5G" | "LTE";
  bands: FrequencyBand[];

  coverage: {
    coastal: number;      // 해안으로부터 킬로미터
    port: boolean;        // 항내 커버리지
  };

  performance: {
    bandwidth: number;    // Mbps
    latency: number;      // ms (일반적으로 20-50ms)
    reliability: number;  // 백분율
  };
}

class AdaptiveConnectivityManager {
  private satellite: VSATSystem;
  private cellular: CellularNetwork;
  private currentNetwork: "satellite" | "cellular";

  selectOptimalNetwork(position: GeoPosition): "satellite" | "cellular" {
    const distanceToCoast = this.calculateDistanceToCoast(position);

    // 커버리지 내에서 사용 가능한 경우 셀룰러 사용
    if (distanceToCoast < 20 && this.cellular.isAvailable()) {
      const cellularQuality = this.cellular.measureQuality();

      // 낮은 지연 및 비용을 위해 셀룰러 선호
      if (cellularQuality.signalStrength > -90) {
        return "cellular";
      }
    }

    // 외해이거나 셀룰러 사용 불가 시 위성 사용
    return "satellite";
  }

  async seamlessHandover(from: "satellite" | "cellular", to: "satellite" | "cellular"): Promise<void> {
    console.log(`${from}에서 ${to}로 핸드오버 시작`);

    // 1. 새 연결 설정
    const newConnection = await this.establishConnection(to);

    // 2. 데이터 스트림 일시적으로 복제
    await this.duplicateStreams(from, to);

    // 3. 새 연결 안정성 확인
    await this.verifyConnection(to, duration: 10000);  // 10초

    // 4. 이전 연결 종료
    await this.terminateConnection(from);

    this.currentNetwork = to;
    console.log(`핸드오버 완료. 현재 ${to} 사용 중`);
  }
}
```

### 원격 모니터링 인터페이스

운영자 인터페이스는 포괄적인 선박 인식을 제공합니다.

```typescript
interface MonitoringDashboard {
  // 항해 디스플레이
  navigation: {
    chart: ElectronicChart;
    ownShipPosition: GeoPosition;
    plannedRoute: Route;
    actualTrack: TrackHistory;
    nearbyVessels: VesselDisplay[];
    ais: AISOverlay;
  };

  // 센서 시각화
  sensors: {
    radarDisplay: RadarPPI;       // 평면 위치 표시기
    lidarVisualization: PointCloudView;
    cameraFeeds: CameraGrid;
    fusedView: SensorFusionVisualization;
  };

  // 시스템 상태
  systems: {
    propulsion: PropulsionStatus;
    steering: SteeringStatus;
    power: PowerSystemStatus;
    navigation: NavigationSystemStatus;
    communication: CommunicationStatus;
    autonomy: AutonomyStatus;
  };

  // 경보 및 경고
  alerts: AlertPanel;

  // 성능 메트릭
  metrics: {
    fuelConsumption: MetricDisplay;
    speed: MetricDisplay;
    eta: MetricDisplay;
    weatherConditions: WeatherDisplay;
  };
}
```

### 원격 제어 모드

육상 운영자는 다양한 수준에서 개입할 수 있습니다:

```typescript
enum ControlMode {
  FULLY_AUTONOMOUS = "fully_autonomous",     // 선박이 독립적으로 운영
  SUPERVISED = "supervised",                  // 운영자가 모니터링, 개입 가능
  REMOTE_CONTROLLED = "remote_controlled",   // 운영자가 선박을 직접 제어
  EMERGENCY = "emergency"                     // 비상 재정의 모드
}

class RemoteControlSystem {
  private currentMode: ControlMode = ControlMode.SUPERVISED;

  transitionToRemoteControl(reason: string): void {
    console.log(`원격 제어로 전환: ${reason}`);

    // 1. 자율 시스템에 알림
    this.sendCommand({
      type: "mode_transition",
      from: this.currentMode,
      to: ControlMode.REMOTE_CONTROLLED,
      timestamp: new Date()
    });

    // 2. 확인 대기
    const ack = await this.waitForAcknowledgment(timeout: 5000);

    if (!ack) {
      console.error("선박이 제어 전환을 확인하지 못함");
      this.initiateEmergencyMode();
      return;
    }

    // 3. 제어 권한 확인
    const controlVerified = await this.verifyControl();

    if (controlVerified) {
      this.currentMode = ControlMode.REMOTE_CONTROLLED;
      console.log("원격 제어 설정됨");
    } else {
      console.error("원격 제어 설정 실패");
      this.initiateEmergencyMode();
    }
  }
}
```

### 운영자 교육 및 자격

원격 운영자는 전문 교육이 필요합니다:

**교육 프로그램:**
```typescript
interface RemoteOperatorTraining {
  prerequisites: {
    maritimeLicense: "master" | "chief_officer" | "second_officer";
    seaExperience: number;  // 년
    vesselTypes: VesselType[];
  };

  courses: {
    autonomousSystemsOverview: {
      duration: "5일";
      topics: [
        "자율 항해 원리",
        "센서 시스템 및 한계",
        "AI 의사결정 개요",
        "통신 시스템"
      ];
    };

    remoteControlOperations: {
      duration: "10일";
      topics: [
        "육상 제어 센터 시스템",
        "원격 모니터링 기술",
        "제어 모드 전환",
        "비상 절차"
      ];
    };

    simulatorTraining: {
      duration: "20일";
      scenarios: [
        "정상 항해 모니터링",
        "원격 제어 인수",
        "비상 대응",
        "시스템 고장",
        "악천후",
        "다중 선박 충돌"
      ];
    };

    onVesselFamiliarization: {
      duration: "5일";
      activities: [
        "자율 시스템 검사",
        "센서 교정",
        "수동 재정의 절차",
        "시스템 테스트"
      ];
    };
  };

  certification: {
    writtenExam: { passingScore: 80 };
    practicalExam: { simulatorScenarios: 10 };
    vesselSpecificQualification: { requiredHours: 40 };
  };
}
```

### 한국의 육상 제어 센터 개발

**HMM 스마트 오퍼레이션 센터:**
- 서울/부산에 위치
- 선박 모니터링 및 최적화
- 연료 효율 관리
- 예측 유지보수

**한국조선해양 통합 운영 센터:**
- 스마트십 기술 통합
- 원격 진단 시스템
- AI 기반 의사결정 지원
- 24/7 기술 지원

**삼성중공업 자율운항 관제센터:**
- 자율운항 LNG선 모니터링
- 실시간 성능 분석
- 원격 제어 기능
- 사이버보안 모니터링

### 데이터 대역폭 요구사항

**원격측정 데이터 스트림:**
```typescript
interface DataBandwidthRequirements {
  // 중요 데이터 (연속)
  critical: {
    position: {
      updateRate: "1 Hz";
      bandwidth: "0.1 kbps";
    };
    courseSpeed: {
      updateRate: "1 Hz";
      bandwidth: "0.1 kbps";
    };
    systemStatus: {
      updateRate: "1 Hz";
      bandwidth: "1 kbps";
    };
  };

  // 센서 데이터 (연속)
  sensors: {
    radarProcessed: {
      updateRate: "2 Hz";
      bandwidth: "50 kbps";      // 원시 데이터가 아닌 처리된 트랙
    };
    aisData: {
      updateRate: "0.5 Hz";
      bandwidth: "10 kbps";
    };
    weatherSensors: {
      updateRate: "0.1 Hz";
      bandwidth: "1 kbps";
    };
  };

  // 비디오 스트림 (온디맨드 또는 저하)
  video: {
    forwardCamera: {
      resolution: "1920x1080";
      frameRate: "15 fps";       // 대역폭을 위해 30에서 감소
      compression: "H.265";
      bandwidth: "3 Mbps";
    };
    aftCamera: {
      resolution: "1280x720";
      frameRate: "10 fps";
      compression: "H.265";
      bandwidth: "1.5 Mbps";
    };
  };

  // 총 요구사항
  total: {
    minimum: "62 kbps";          // 중요 + 센서만
    standard: "70 kbps + 비디오 스트림 2개 (6.5 Mbps)";
    full: "70 kbps + 비디오 스트림 6개 (14 Mbps)";
  };
}
```

### 원격 운영을 위한 사이버보안

```typescript
class RemoteControlSecurity {
  private encryptionKey: CryptoKey;
  private authToken: AuthenticationToken;

  async authenticateOperator(credentials: OperatorCredentials): Promise<boolean> {
    // 다중 요소 인증
    const factors: AuthenticationFactor[] = [
      { type: "password", value: credentials.password },
      { type: "certificate", value: credentials.clientCertificate },
      { type: "token", value: credentials.hardwareToken }
    ];

    const authResults = await Promise.all(
      factors.map(f => this.verifyFactor(f))
    );

    // 모든 요소가 성공해야 함
    return authResults.every(r => r === true);
  }

  encryptCommand(command: ControlCommand): EncryptedPayload {
    // AES-256-GCM 암호화
    const payload = JSON.stringify(command);
    const nonce = this.generateNonce();

    const encrypted = crypto.encrypt({
      algorithm: "AES-256-GCM",
      key: this.encryptionKey,
      nonce,
      data: payload
    });

    // 무결성을 위한 HMAC 추가
    const hmac = this.calculateHMAC(encrypted);

    return {
      ciphertext: encrypted,
      nonce,
      hmac,
      timestamp: Date.now()
    };
  }

  validateCommandIntegrity(payload: EncryptedPayload): boolean {
    // 타임스탬프 확인 (재생 공격 방지)
    const age = Date.now() - payload.timestamp;
    if (age > 5000) {  // 최대 5초
      console.error("명령이 너무 오래됨, 재생 공격 가능성");
      return false;
    }

    // HMAC 확인
    const expectedHMAC = this.calculateHMAC(payload.ciphertext);
    if (expectedHMAC !== payload.hmac) {
      console.error("HMAC 불일치, 데이터 무결성 손상");
      return false;
    }

    return true;
  }
}
```

### 철학: 弘益人間

원격 모니터링 및 제어는 다음과 같이 弘益人間을 구현합니다:
- 선원을 더 안전한 육상 직업으로 전환
- 해상에 물리적으로 존재하지 않고도 인간 전문 지식 활용
- 사고를 예방하는 감독 제공
- 효율성을 위한 함대 운영 최적화
- 새로운 숙련된 고용 기회 창출

---

**다음 장:** 충돌 회피 시스템 - 실시간 위협 감지 및 자율 회피 기동.

---

© 2025 SmileStory Inc. / WIA
弘益人間 (홍익인간) · Benefit All Humanity
