openapi: 3.1.0
info: { title: WIA-DIGITAL-ERASURE, version: 1.0.0 }
servers: [{ url: https://api.example.com/wia-digital-erasure/v1 }]
paths:
  /erasure-request:
    post: { summary: Submit erasure request, responses: { '201': { description: Created } } }
  /erasure-attestation/{id}:
    get: { summary: Retrieve erasure attestation, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: OK } } }
  /audit:
    get: { summary: Audit log query, responses: { '200': { description: OK } } }
