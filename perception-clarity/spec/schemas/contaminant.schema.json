{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wiastandards.com/perception-clarity/spec/schemas/contaminant.schema.json",
  "title": "Contaminant",
  "description": "A single detected contaminant affecting a sensor's optical/sensing window.",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "rain_film",
        "mud_dust",
        "salt_spray",
        "insect_strike",
        "frost_ice",
        "condensation",
        "sun_glare",
        "scratch_abrasion",
        "other"
      ],
      "description": "Contaminant classification. Use 'other' with a note for unclassified contamination."
    },
    "coverage": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Fraction of the sensing window affected by this contaminant."
    },
    "note": {
      "type": "string",
      "description": "Optional free-text description (recommended when type is 'other')."
    }
  },
  "required": ["type", "coverage"],
  "additionalProperties": false
}
