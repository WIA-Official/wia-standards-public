# WIA Citation Veracity Git Hooks (public)

Install:

```bash
git config core.hooksPath scripts/git-hooks
```

Per `docs/CITATION-POLICY.md` v1.0 §4.2. Blocks commits introducing
forbidden citation patterns into standard subdirectories.
