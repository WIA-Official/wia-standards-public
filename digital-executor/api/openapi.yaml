openapi: 3.1.0
info: { title: WIA digital-executor, version: 1.0.0 }
servers: [{ url: https://api.example.com/wia-digital-executor/v1 }]
paths:
  /resource:
    get: { summary: List, responses: { '200': { description: OK } } }
    post: { summary: Create, responses: { '201': { description: Created } } }
  /resource/{id}:
    get: { summary: Retrieve, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: OK } } }
