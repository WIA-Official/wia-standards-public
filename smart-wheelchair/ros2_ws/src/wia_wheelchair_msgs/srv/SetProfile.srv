# SetProfile.srv
# WIA Smart Wheelchair 사용자 프로파일 적용 서비스

# Request
uint8 profile_id                      # 프로파일 ID (0-255)
string profile_name                   # 프로파일 이름 (빈 문자열이면 ID 사용)

---

# Response
bool success
string message
uint8 active_profile_id               # 활성화된 프로파일 ID
string active_profile_name            # 활성화된 프로파일 이름
